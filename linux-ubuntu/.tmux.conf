# bar color
set -g status-bg black
set -g status-fg green

# terminal for coloring
set -g default-terminal "xterm"
set -g default-terminal "xterm-256color"

# Start windows and panes as 1, not 0
set -g base-index 1
setw -g pane-base-index 1
